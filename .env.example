# Flask / Application
FLASK_ENV=production
SECRET_KEY=change-me
DATABASE_URL=sqlite:///instance/plans.db
ADMIN_DEFAULT_PASSWORD=change-me-too

# Optional host/port overrides for waitress
HOST=0.0.0.0
PORT=5000
